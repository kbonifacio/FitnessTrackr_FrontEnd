import react from "react";
import './MyRoutines.css';

const MyRoutines = () =>{
    return (
       <div className="MainContainer"> "I will guide others on the Warrior Path! This should only be show if a user is logged in."</div>
    )
};

export default MyRoutines;